# FlyCommanderByVision frozen NUC baseline — 2026-09-24

Stage 0 PASS; Stage 1 PASS/FROZEN; Stage 2 PASS/FROZEN (Run B development
calibration); Stage 3 PASS/FROZEN; Stage 4 PASS/FROZEN; Stage 5 V2.2
SINGLE-OPERATOR DEMO BASELINE PASS, full multi-person safety deferred;
Stage 6A PASS; Stage 6B PASS. Gate 6C evidence tooling READY, but formal
five-gesture aircraft-response test NOT STARTED and must be repeated after
Safety Pilot Flight Authority integration. Stage 6 overall is NOT PASS.

The Safety Pilot owns ARM/TAKEOFF/OFFBOARD/HOLD/LAND. Stage 5 owns identity
and Session/Gallery. Stage 6 owns only a short-lived Motion Lease and
requires actual fresh armed+Offboard+no-failsafe PX4 state for live output.
Leaving Offboard revokes movement without deleting identity; re-entry
requires trusted neutral release followed by a new gesture. Stage 1 Gateway
and PX4 failsafe settings are unchanged. No RK3576 implementation or
physical-drone testing is part of this baseline.

The repository pins source, configuration, tests, model hashes and external
commit IDs. It intentionally excludes binaries, datasets, calibration
captures, videos, rosbag/ULog and build/venv artifacts. See README,
`requirements-nuc.txt`, `models/manifest.json`, `THIRD_PARTY_NOTICES.md`,
and Stage-specific validation reports.
