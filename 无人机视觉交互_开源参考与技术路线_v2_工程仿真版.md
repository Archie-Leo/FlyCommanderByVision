# 无人机视觉交互系统：工程仿真底座、开源参考与完整技术路线（V2）

> 更新日期：2026-09-09  
> 版本定位：从“视觉算法逐步接无人机”的路线，升级为“**以 PX4 SITL + Gazebo + ROS 2 为数字飞行底座，真实摄像头与真实视觉算法直接进入闭环**”的工程开发总纲。  
> 核心目标：尽量复用成熟飞控、物理仿真和通信基础设施，把团队的自主工作集中在 **Pose / Tracking / Operator Lock / Gesture / Unknown Reject / Intent / Safety** 等视觉交互问题上，而不是自行实现四旋翼控制器。

> **当前工程实况覆盖（2026-09-09）**：正式 NUC `sentinel-S600` 已冻结为
> Ubuntu 22.04.5 + ROS 2 Humble + Gazebo Harmonic 8.15.0 + PX4 v1.17.0；
> 该组合已经通过 Stage 0 和官方 Offboard Baseline，不因本文早期的 24.04/Jazzy
> 推荐而擅自升级。S1-02 Control Gateway V1 已在远端
> `~/ros2_px4_ws/src/drone_control_gateway` 完成并通过 Gazebo X500 实飞 Gate，
> V1 稳定 commit 为 `e88b16459b812cf59d824e6c7afbe9de8db1d725`；
> 2026-09-10 V1.1 又完成 world NED +X/-X 与 yaw-rate 实飞验收，Stage 1
> 正式 PASS 并冻结控制层，当前 commit 为
> `09776d3d234ccebabb5c86cf7aed7be1026bfe30`。细节见
> `PROJECT_STATUS.md` 与执行清单。

---

# 0. 总原则：我们不做“玩具仿真”，也不自己重写飞控

项目从第一天开始就按最终系统的工程边界开发：

```text
现实中的人 + 真实摄像头 + 真实视觉算法
                     ↓
              Intent / Safety
                     ↓
                 ROS 2
                     ↓
                  PX4
                     ↓
          Gazebo 四轴物理世界
```

其中：

- **真实部分**：操作者、USB/CSI 摄像头、视觉算法、动作识别、安全状态机、ROS 2 业务节点；
- **仿真部分**：机体、传感器、动力学、重力、碰撞、风、虚拟环境；
- **真实飞控逻辑**：PX4 SITL 中运行正常的 commander、位置/速度控制、姿态控制、角速度控制、Mixer/Actuator 等飞控软件链；
- **我们不做**：自写姿态 PID、自写电机混控、自写四旋翼动力学、自写 IMU/GPS 模拟。

> 对视觉组而言，PX4 + Gazebo 是“数字无人机实验台”，不是比赛最后才加的展示效果。

---

# 1. 第一核心架构：真实视觉 × 数字飞行器的混合闭环

```text
                     【现实世界】

          ┌─────────────────────────┐
          │          操作者          │
          └────────────┬────────────┘
                       │
                  USB / CSI Camera
                       │
                       ▼
          ┌─────────────────────────┐
          │      Vision Runtime      │
          │                         │
          │  Person / Pose          │
          │       ↓                 │
          │  Tracking               │
          │       ↓                 │
          │  Operator Lock          │
          │       ↓                 │
          │  Pose Normalize         │
          │       ↓                 │
          │  Gesture / Action       │
          │       ↓                 │
          │  Temporal Stabilizer    │
          │       ↓                 │
          │  Unknown Reject         │
          └────────────┬────────────┘
                       │
                       ▼
          ┌─────────────────────────┐
          │   Intent + Safety FSM    │
          └────────────┬────────────┘
                       │
                       ▼
          ┌─────────────────────────┐
          │    ROS 2 Control Gateway │
          │                         │
          │ Intent → PX4 Setpoint   │
          └────────────┬────────────┘
                       │
                 uXRCE-DDS / ROS 2
                       │
───────────────────────┼────────────────────────
                       │
                       ▼
                    【PX4】

          Position / Velocity Control
                       ↓
                Attitude Control
                       ↓
                  Rate Control
                       ↓
              Mixer / Actuator
                       │
                       ▼
              【Gazebo Harmonic】

          X500 Quadrotor Dynamics
          ├─ Motor / thrust model
          ├─ gravity / inertia
          ├─ collision
          ├─ IMU / GPS / barometer
          ├─ wind / disturbances
          └─ virtual camera / depth / lidar
```

这张图是项目的**主架构图**。后续换 Pose 模型、换动作识别器、换 Jetson 或换真实无人机，都不应破坏这条主链。

---

# 2. 为什么把仿真平台提升为 Stage 0

原先容易采用的开发顺序是：

```text
Pose → Gesture → FSM → 最后再接无人机
```

V2 改为：

```text
先建立可飞的数字无人机
        ↓
再把视觉一层层接进去
```

原因：

1. **视觉输出从第一天就有真实的系统终点**，不会做出无法落地的 Gesture API；
2. 可以立即测端到端延迟、控制抖动、超时和 failsafe；
3. 飞控与物理模型由成熟系统承担，避免视觉组陷入控制专业细节；
4. 后续接真机时，视觉链和 Intent 协议基本不需要重写；
5. 每个视觉算法改动都可以在完整闭环里做 A/B 测试，而不是只看离线 Accuracy。

---

# 3. 主开发环境冻结建议

截至 2026-09-09，PX4 官方文档推荐的 ROS 2 平台为：

```text
Ubuntu 24.04
ROS 2 Jazzy LTS
Gazebo Harmonic
PX4 SITL
QGroundControl
```

官方参考：

- PX4 ROS 2 User Guide  
  https://docs.px4.io/main/en/ros2/user_guide

- PX4 Ubuntu Development Environment  
  https://docs.px4.io/main/en/dev_setup/dev_env_linux_ubuntu

- PX4 Gazebo Simulation  
  https://docs.px4.io/main/en/sim_gazebo_gz/

- ROS/Gazebo bridge  
  https://gazebosim.org/docs/harmonic/ros2_overview/

## 3.1 推荐角色分工

### NUC / x86 工作站

负责：

```text
PX4 SITL
Gazebo
ROS 2
QGroundControl
视觉算法开发
数据记录 / 回放
训练与评估
```

### Jetson

先不作为完整开发环境的约束条件，而作为后期：

```text
Edge Runtime
    ↓
ONNX / TensorRT
    ↓
Real Camera + Vision
```

这样不会因为 JetPack、CUDA、ARM wheel 或旧版 Ubuntu 反向绑死整个项目。

## 3.2 版本管理原则

不要让每个人追最新包。

工程首次完整 PASS 后冻结：

```text
OS version
ROS distro
PX4 tag / commit hash
Gazebo version
Python version
model weight hash
requirements lock
config hash
```

正式比赛前禁止现场临时：

```text
pip install -U ...
apt full-upgrade
切 PX4 main 新 commit
```

---

# 4. Stage 0：PX4 + Gazebo + ROS 2 工程仿真底座

这是项目第一个正式 Gate，**视觉算法还没有进入系统**。

## 4.1 官方起点

最基础 X500：

```bash
make px4_sitl gz_x500
```

带单目相机：

```bash
make px4_sitl gz_x500_mono_cam
```

带深度相机：

```bash
make px4_sitl gz_x500_depth
```

官方模型索引：

https://docs.px4.io/main/en/sim_gazebo_gz/vehicles

## 4.2 Simulation Baseline Gate

只有下面全部 PASS，仿真底座才算冻结：

```text
[ ] Gazebo X500 正常生成
[ ] PX4 SITL 正常启动
[ ] QGroundControl 正常连接
[ ] ROS 2 能看到 PX4 topics
[ ] ROS 2 → PX4 指令通信正常
[ ] ARM / DISARM 正常
[ ] OFFBOARD 切换正常
[ ] TAKEOFF 正常
[ ] HOVER 稳定
[ ] X/Y/Z 位置控制正常
[ ] Velocity 控制正常
[ ] Yaw 控制正常
[ ] LAND 正常
[ ] Offboard heartbeat 丢失时 failsafe 行为符合预期
[ ] ROS 2 / PX4 / Gazebo 时间戳可对齐
[ ] ULog / rosbag2 可记录并回放关键数据
```

> Gate 通过后，视觉组默认“飞行底座可用”，不再把日常精力放到 PX4 内部控制器上。

---

# 5. Stage 1：ROS 2 Offboard 控制闭环

PX4 官方 ROS 2 Offboard 示例已经提供：

```text
OffboardControlMode
TrajectorySetpoint
VehicleCommand
```

基本过程：

```text
ROS 2 node
   ↓
持续发送 OffboardControlMode
   ↓
切换 OFFBOARD + ARM
   ↓
发送 TrajectorySetpoint
   ↓
PX4 内部控制器
   ↓
Gazebo X500
```

官方示例：

https://docs.px4.io/main/en/ros2/offboard_control

## 5.1 我们不要控制到电机层

视觉组的控制边界只做到：

```text
position setpoint
velocity setpoint
yaw / yaw rate
high-level vehicle command
```

不要在视觉链中直接产生：

```text
motor PWM
raw actuator
rate controller output
PID gains
```

## 5.2 Control Gateway

新增正式模块：

```text
IntentMessage
     ↓
Control Gateway
     ↓
PX4-safe setpoint
```

示例：

```text
MOVE_RIGHT
    ↓
body/world frame mapping
    ↓
velocity setpoint

ASCEND
    ↓
vertical velocity / altitude setpoint

HOVER
    ↓
zero velocity or hold current position

LAND
    ↓
PX4 landing command / mode
```

这里必须显式处理：

- ROS 常见 ENU 与 PX4 NED 坐标系差异；
- world frame / body frame 差异；
- 正负方向定义；
- 最大速度、最大加速度、最大 yaw rate；
- 命令持续时间；
- setpoint timeout；
- Offboard keep-alive。

> PX4 官方明确提醒 ROS 与 PX4 的坐标系假设不同，不会自动完成所有 frame 转换。这一项必须单元测试。

---

# 6. Stage 2：真实摄像头混合仿真（Hardware-Camera-in-Simulation）

> 2026-09-14 状态更新：Stage 2 Camera Geometry Gate 已 PASS。正式输入为
> `/dev/video0` 的 2560×960 MJPEG SBS（LEFT/RIGHT 各 1280×960）；当前开发默认参数为
> Run B（排除 pair 4/27，原始 captures 不变）。标定、rectification、StereoSGBM、
> Q→3D 与 0.5–2.5 m 静态纹理目标测距已验证。完整结论和限制见
> `STAGE2_STEREO_CAMERA_FINAL.md`。Gazebo/PX4/ROS 2 的视觉闭环仍按 Stage 6 执行，
> 不在 Stage 2 继续扩展。

这是本项目最重要的实验形态。

```text
真实操作者
    ↓
真实 USB Camera
    ↓
真实 OpenCV / ROS Camera Pipeline
    ↓
真实 Vision Runtime
    ↓
真实 Gesture Prediction
    ↓
真实 Intent FSM
    ↓
ROS 2
    ↓
PX4 SITL
    ↓
Gazebo X500
```

这不是 synthetic vision。

除了飞机和环境，**视觉链全部是真实运行**。

## 6.1 第一阶段演示目标

```text
操作者进入摄像头
      ↓
Operator Lock
      ↓
做 RIGHT 动作
      ↓
Gesture = RIGHT
      ↓
稳定确认
      ↓
Intent = MOVE_RIGHT
      ↓
Control Gateway
      ↓
PX4 velocity setpoint
      ↓
Gazebo X500 右移
```

同时：

```text
UNKNOWN              → 不动作
non-operator gesture → 不动作
operator lost        → HOVER
vision timeout       → HOVER / failsafe
```

这条链路跑通，项目主架构就成立。

---

# 7. 双 Camera 架构

## Camera A：真实操作者摄像头

用途：

```text
Human → Drone Interaction
```

即：

```text
人 → Pose → Gesture → Intent → Flight
```

这是当前主线。

## Camera B：Gazebo 虚拟机载摄像头

用途：

```text
Drone → Environment Perception
```

可后续扩展：

- 环境感知；
- 障碍物；
- 目标识别；
- 人体重新搜索；
- 精确降落；
- 深度估计。

**Camera B 属于扩展能力，不允许阻塞第一阶段 Human → Drone 主线。**

---

# 8. 仿真人体 / Actor：用于自动回归，不替代真实摄像头主测试

Gazebo Actor 支持：

- COLLADA `.dae` 骨骼动画；
- BVH `.bvh` 动作；
- scripted trajectory。

参考：

https://gazebosim.org/docs/garden/actors/

因此后期可以建立：

```text
Actor RIGHT
Actor LEFT
Actor HAND_UP
Actor WALK
Actor TURN
Actor RANDOM_INVALID
```

然后让 Gazebo 的虚拟相机观看 Actor，自动跑视觉回归。

## 8.1 Actor 的定位

Actor 非常适合：

- 重复性测试；
- 固定动作 benchmark；
- 摄像机距离/视角 sweep；
- 回归测试；
- synthetic data 辅助。

但不替代：

```text
真实人 + 真实 USB Camera
```

因为比赛最终要面对真实人体、衣服、光照、动作幅度和摄像头噪声。

## 8.2 一个需要注意的限制

Gazebo Actor 主要是动画对象，官方文档说明其本身不按普通动态刚体方式承受重力和接触力。因此：

- 把 Actor 当视觉目标没有问题；
- 如果需要真实人体碰撞/推挤/物理交互，要另加碰撞几何或使用普通动态模型。

---

# 9. Vision Runtime 模块边界

## M1. Camera / Frame Source

统一输出：

```python
FramePacket(
    timestamp_ms,
    frame_id,
    image_bgr,
    width,
    height,
    source_id
)
```

输入可来自：

- USB 摄像头；
- CSI 摄像头；
- ROS 2 Image；
- Gazebo camera；
- 录像回放。

后端算法不关心相机来源。

---

## M2. Person / Pose Perception

输出：

```python
PersonPose(
    local_detection_id,
    bbox_xyxy,
    keypoints_xy,
    keypoints_score,
    pose_score
)
```

候选：

- MediaPipe Pose Landmarker；
- RTMPose / MMPose；
- Ultralytics YOLO Pose。

模块不负责判断 Gesture。

---

## M3. Tracking + Operator Lock

目标：

> 多人场景下确定“谁拥有控制权”。

输出：

```python
TrackedPerson(
    track_id,
    pose,
    is_operator,
    track_age,
    lost_frames
)
```

Baseline：

- ByteTrack；
- BoT-SORT；
- 其他可替换 tracker。

关键问题不是单纯 Track ID，而是：

```text
Acquire control
Maintain ownership
Reject bystander
Release control
Recover operator
```

这是比赛很适合自主设计的部分。

---

## M4. Pose Normalization

把像素坐标转换为与：

- 人体身高；
- 摄像头距离；
- 分辨率；
- 平移位置；

弱相关的骨骼表示。

建议包含：

```text
body center
shoulder / torso scale
joint angles
bone vectors
confidence quality
```

统一输出：

```python
NormalizedSkeleton(
    timestamp_ms,
    track_id,
    joints,
    angles,
    quality
)
```

---

## M5. Gesture / Action Recognizer

### V0：规则 Baseline

```text
Normalized Pose
       ↓
angles / relative positions
       ↓
GestureCandidate
```

目的不是最终方案，而是快速建立可解释 Baseline。

### V1：轻量时序模型

```text
T frames × J joints × (x,y,conf)
                ↓
               TCN
                ↓
GestureCandidate
```

### V2：Skeleton Action Recognition

候选：

- ST-GCN；
- 2s-AGCN；
- PoseC3D；
- 其他轻量 skeleton temporal model。

原则：

> 模型必须通过同一个 Recognizer 接口，不得把 MMAction2 等研究框架直接侵入 Main Runtime。

---

## M6. Temporal Stabilizer + Unknown Reject

这是安全核心。

输出：

```python
StableGesture(
    label,
    confidence,
    stable_ms,
    valid,
    reject_reason
)
```

必须具有：

```text
UNKNOWN
INVALID
TIMEOUT
```

系统目标不是每帧都分类，而是：

> **宁可拒绝，也不能把未知动作误接受成飞行指令。**

---

## M7. Intent + Safety FSM

视觉结果永远不能直接变成 PX4 setpoint。

推荐状态：

```text
IDLE
  ↓ activation
OPERATOR_LOCKED
  ↓
READY
  ↓ stable gesture
COMMAND_CONFIRMED
  ↓
EXECUTING
  ↓ gesture release / timeout
HOVER
```

任何时候：

```text
operator lost      → HOVER
vision timeout     → HOVER
ambiguous gesture  → NO_COMMAND / HOVER
control timeout    → PX4 failsafe path
emergency          → safe emergency policy
```

统一输出：

```python
IntentMessage(
    timestamp_ms,
    operator_id,
    intent,
    confidence,
    valid,
    seq,
    reason
)
```

---

# 10. Interface / Protocol：防止后期拼不起来

建议继续保留稳定接口：

## PoseFrame

```python
@dataclass
class PoseFrame:
    timestamp_ms: int
    frame_id: int
    track_id: int
    bbox_xyxy: tuple
    keypoints_xy: list
    keypoints_conf: list
    pose_score: float
```

## GestureCandidate

```python
@dataclass
class GestureCandidate:
    timestamp_ms: int
    track_id: int
    label: str
    confidence: float
```

## StableGesture

```python
@dataclass
class StableGesture:
    timestamp_ms: int
    operator_id: int
    label: str
    confidence: float
    stable_ms: int
    valid: bool
    reject_reason: str
```

## IntentMessage

```python
@dataclass
class IntentMessage:
    timestamp_ms: int
    operator_id: int
    intent: str
    confidence: float
    valid: bool
    seq: int
    reason: str
```

推荐 intent 枚举：

```text
NONE
TAKEOFF
LAND
HOVER
ASCEND
DESCEND
MOVE_LEFT
MOVE_RIGHT
MOVE_FORWARD
MOVE_BACKWARD
YAW_LEFT
YAW_RIGHT
EMERGENCY_STOP
```

---

# 11. Control Gateway / Drone Adapter

保留 Adapter 思想，但 PX4 成为主实现。

```python
class DroneAdapter:
    def connect(self): ...
    def arm(self): ...
    def disarm(self): ...
    def takeoff(self): ...
    def land(self): ...
    def hover(self): ...
    def set_velocity(self, vx, vy, vz, yaw_rate): ...
    def get_state(self): ...
```

实现：

```text
DroneAdapter
 ├─ PX4SimAdapter       ← 主开发路径
 ├─ PX4RealAdapter      ← 后期真机
 ├─ TelloAdapter        ← 可选快速实体 Demo
 └─ ReplayAdapter       ← 离线回放/测试
```

> Tello 不再作为主架构的飞行底座；如果团队已有 Tello，可以作为低风险快速实体验证设备，但不能反过来定义主协议。

---

# 12. 时间同步：必须从第一天设计

仿真系统至少涉及：

```text
Camera clock
ROS 2 clock
Gazebo simulation clock
PX4 clock
```

如果时间不统一，下面指标都会失真：

- gesture latency；
- end-to-end latency；
- state alignment；
- rosbag replay；
- sensor fusion；
- timeout logic。

PX4 官方 ROS 2 指南给出一种场景：把 Gazebo `/clock` 桥接到 ROS 2，并让 ROS 2 节点启用：

```text
use_sim_time = true
```

同时相应配置 PX4 uXRCE-DDS 时间同步，使 Gazebo 成为统一仿真时间源。

参考：

https://docs.px4.io/main/en/ros2/user_guide

## 时间戳规范

所有核心消息必须携带时间：

```text
Frame
Pose
Track
Gesture
Intent
VehicleState
```

日志里不能只记录“处理耗时”，还要能计算跨节点端到端延迟。

---

# 13. ROS 2 通信原则

不要让 ROS message 类型渗透到全部算法内部。

推荐：

```text
Pure Python/C++ domain object
        ↕ thin wrapper
ROS2 message
```

例如：

```text
Vision algorithm
   ↓
IntentMessage (domain)
   ↓
intent_ros_node
   ↓
ROS topic
```

这样：

- 离线评估不用 ROS；
- 单元测试不用启动 Gazebo；
- Jetson Runtime 可以独立部署；
- 网络通信后端可替换。

## 建议 topic 层次

```text
/camera/operator/image
/vision/pose
/vision/tracks
/vision/operator
/vision/gesture_raw
/vision/gesture_stable
/interaction/intent
/interaction/safety_state
/px4/...               # PX4 官方 topics
```

QoS 策略应按用途区分：

- Camera / high-rate perception：Sensor Data 风格，优先低延迟；
- Intent / safety command：强调可靠性与可观测性；
- Debug visualization：不阻塞主链。

---

# 14. Debug / Logging / Replay：工程系统必须具备

建议工具：

```text
rosbag2
PX4 ULog
QGroundControl
RViz2
Foxglove（可选）
```

每次关键实验至少记录：

```text
camera timestamp
frame id
track id
operator id
keypoints
pose quality
gesture raw
gesture confidence
stable_ms
unknown reason
FSM state
Intent
control setpoint
PX4 nav state
position
velocity
attitude
failsafe state
```

目标是任何异常都能回答：

> “到底是 Camera、Pose、Tracking、Gesture、FSM、Control Gateway 还是 PX4 出错？”

推荐回放链：

```text
rosbag / video
     ↓
offline Vision replay
     ↓
Gesture replay
     ↓
FSM replay
     ↓
Intent comparison
```

这样一次失败实验可以反复复现，不需要操作者重新做动作。

---

# 15. Fault Injection：把安全测试变成正式实验

## 视觉层

```text
camera disconnect
camera delay
frame drop
low FPS
pose lost
low-confidence joints
tracker ID switch
false gesture
```

## 人员层

```text
1 / 2 / 3 people
operator crossing
bystander gesture attack
operator leaves frame
partial occlusion
back-facing / side-facing
random invalid motion
```

## 通信层

```text
ROS topic delay
topic timeout
node crash
Intent packet loss
Offboard heartbeat loss
```

## 飞行层

```text
wind disturbance
GPS abnormality
sensor fault
position drift
failsafe activation
```

完整安全测试的目标不是“始终识别成功”，而是：

```text
识别成功 → 正确执行
识别不确定 → 拒绝
视觉消失 → 安全悬停 / failsafe
非操作者 → 不响应
通信异常 → 不继续危险动作
```

---

# 16. 指标体系：从视觉 Accuracy 升级成系统指标

## 视觉基础

- Pose FPS；
- Pose latency；
- 关键点缺失率；
- track ID switch；
- operator reacquisition time。

## Gesture

- per-class Precision / Recall；
- confusion matrix；
- Unknown rejection rate；
- False Acceptance Rate；
- False Trigger Rate；
- stable confirmation delay。

## Interaction

- operator acquisition time；
- gesture → intent latency；
- invalid gesture rejection latency；
- operator lost → hover latency。

## Control / System

- intent → PX4 setpoint latency；
- setpoint → vehicle response latency；
- end-to-end latency；
- Offboard loss → failsafe latency；
- command overshoot / oscillation（只做系统观测，不自行重写 PX4 内环）；
- experiment reproducibility。

---

# 17. 开源项目：按“能力”复用，不整体魔改

## 17.1 系统级参考：AerialRobotics-IITK / gesture-controlled-drone

https://github.com/AerialRobotics-IITK/gesture-controlled-drone

参考重点：

- perception/control 分离；
- gesture 不直接控制电机；
- 状态机；
- 感知失败回退；
- ROS 2 / MAVROS / Gazebo / ArduPilot SITL 完整闭环思路。

不要直接继承它的全部环境或代码结构。

---

## 17.2 数据采集与轻量 Gesture 工程范式：mageSoft / tello-gesture-controlled

https://github.com/mageSoft/tello-gesture-controlled

值得复用的是：

```text
采数据
  ↓
预处理
  ↓
标签
  ↓
训练
  ↓
导出
  ↓
实时推理
```

不要合并它的旧版 TensorFlow / MediaPipe requirements。

---

## 17.3 Debounce / PID 交互思想：jinMori127 / Hand-Drone-Control

https://github.com/jinMori127/Hand-Drone-Control

参考：

- debounce；
- temporal evidence accumulation；
- 人体中心误差 → yaw control 的概念。

不建议继承旧检测器或简单“最近人=操作者”的策略。

---

## 17.4 Tello 快速真机 Adapter：DJITelloPy

https://github.com/damiafuentes/djitellopy

定位调整为：

```text
Optional Physical Demo Adapter
```

而不是主飞控框架。

---

# 18. Pose / Tracking 技术选择

## MediaPipe Pose Landmarker

https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker/python

适合：

- 第一版单人 Pose；
- 快速 Baseline；
- 低成本验证 Gesture 规则。

---

## YOLO Pose + Tracking

https://docs.ultralytics.com/modes/track/

适合：

```text
person + pose + ID
```

快速建立多人 Baseline。

License 注意：Ultralytics 主仓库采用 AGPL / Enterprise 双许可，架构不能锁死在其 Python API。

---

## RTMPose / MMPose

https://github.com/open-mmlab/mmpose

适合：

- 比赛版 Pose 候选；
- ONNX / TensorRT 部署；
- 与 MediaPipe 做 FPS、稳定性和精度对比。

推荐：

```text
MediaPipe = fast baseline
RTMPose   = competition / deployment candidate
```

---

# 19. 时序动作识别研究环境

MMAction2：

https://github.com/open-mmlab/mmaction2

候选：

- ST-GCN；
- 2s-AGCN；
- PoseC3D。

但主运行环境不要直接依赖完整 MMAction2。

推荐：

```text
Research env
   ↓
train / evaluate
   ↓
model.onnx / .pt
   ↓
Main Runtime
```

第一学习型模型优先考虑轻量 TCN，再用 ST-GCN / PoseC3D 做对比。

---

# 20. 推荐工程目录（V2）

```text
drone-vision-interaction/
│
├─ apps/
│  ├─ vision_demo.py
│  ├─ hybrid_sim.py
│  ├─ offline_replay.py
│  └─ full_system.py
│
├─ interfaces/
│  ├─ messages.py
│  ├─ enums.py
│  └─ frames.py
│
├─ perception/
│  ├─ camera/
│  │  ├─ usb_camera.py
│  │  ├─ ros_camera.py
│  │  └─ replay_camera.py
│  ├─ pose/
│  │  ├─ base.py
│  │  ├─ mediapipe_backend.py
│  │  ├─ yolo_pose_backend.py
│  │  └─ rtmpose_backend.py
│  ├─ tracking/
│  │  ├─ base.py
│  │  ├─ bytetrack_backend.py
│  │  └─ botsort_backend.py
│  ├─ operator/
│  │  └─ operator_lock.py
│  └─ normalize/
│     └─ skeleton_normalizer.py
│
├─ recognition/
│  ├─ rules/
│  │  └─ rule_gesture.py
│  ├─ temporal/
│  │  ├─ sliding_window.py
│  │  ├─ tcn_backend.py
│  │  └─ onnx_action_backend.py
│  └─ labels.py
│
├─ interaction/
│  ├─ state_machine.py
│  ├─ rejector.py
│  └─ intent_protocol.py
│
├─ control/
│  ├─ gateway.py
│  ├─ adapter_base.py
│  ├─ px4_sim_adapter.py
│  ├─ px4_real_adapter.py
│  ├─ tello_adapter.py
│  └─ frame_transform.py
│
├─ ros2_ws/
│  └─ src/
│     ├─ vision_bridge/
│     ├─ intent_bridge/
│     └─ px4_control_gateway/
│
├─ simulation/
│  ├─ worlds/
│  ├─ actors/
│  ├─ launch/
│  ├─ fault_scenarios/
│  └─ README.md
│
├─ logging/
│  ├─ recorder.py
│  ├─ replay.py
│  └─ schemas.py
│
├─ evaluation/
│  ├─ gesture_metrics.py
│  ├─ tracking_metrics.py
│  ├─ latency_metrics.py
│  ├─ safety_metrics.py
│  └─ scenario_runner.py
│
├─ configs/
│  ├─ runtime.yaml
│  ├─ gesture.yaml
│  ├─ safety.yaml
│  ├─ px4.yaml
│  └─ simulation.yaml
│
└─ tests/
```

---

# 21. 新开发顺序：Stage 0 → Stage 9

## Stage 0 — 数字飞行底座

```text
PX4 SITL
+ Gazebo Harmonic
+ ROS 2 Humble（当前冻结实况）
+ QGroundControl
```

验收：Simulation Baseline Gate 全 PASS。

---

## Stage 1 — ROS 2 Offboard

```text
ROS 2
 ↓
PX4 Offboard
 ↓
X500 起飞 / 悬停 / XYZ / yaw / 降落
```

禁止自写内环控制器。

---

## Stage 2 — 真实摄像头

**状态：PASS（2026-09-14）。** Stage 3 默认消费 rectified LEFT RGB；RIGHT/depth 作为
可选增强，不要求左右目各跑一套完整 Pose。Run B 是 DEVELOPMENT/PROVISIONAL 默认，
不是生产级最终标定。

```text
USB Camera
 ↓
FramePacket
 ↓
ROS / native runtime
```

记录 FPS、timestamp、丢帧率。

---

## Stage 3 — Pose + Normalize

```text
Camera
 ↓
Rectified LEFT RGB
 ↓
Pose
 ↓
Pose Quality Gate
 ↓
NormalizedSkeleton
```

先单人。Pose 输出必须保留源图像 frame_id/timestamp、关键点像素坐标与 confidence，
并明确任何模型 resize/letterbox 到 1280×960 rectified 坐标的逆映射。RIGHT rectified
图与 depth_mm/valid_mask 仅作为距离、3D 一致性和后续 Operator Ownership gating 的
增强输入；深度无效不得阻塞首版 2D Pose，也不得被当作 0 距离。

---

## Stage 4 — Gesture Baseline

先定义 4–6 个高区分度动作：

```text
Pose
 ↓
Rules
 ↓
GestureCandidate
```

建立第一版 confusion matrix。

---

## Stage 5 — Tracking + Operator Lock + Unknown Reject

此阶段重点从“识别”变成“安全交互”：

```text
多人
 ↓
operator ownership
 ↓
only operator accepted
 ↓
UNKNOWN / timeout reject
```

---

## Stage 6 — Real Camera Hybrid Closed Loop

完整链：

```text
真人
 ↓
USB Camera
 ↓
Vision
 ↓
Gesture
 ↓
Safety FSM
 ↓
Intent
 ↓
ROS 2
 ↓
PX4
 ↓
Gazebo X500
```

这是第一阶段里程碑。

---

## Stage 7 — 自动化压力测试 / Fault Injection

批量测试：

- 人数；
- 距离；
- 角度；
- 遮挡；
- 光照；
- 非法动作；
- 丢帧；
- 延迟；
- operator lost；
- Offboard loss；
- 风扰动。

---

## Stage 8 — Temporal Model Upgrade

```text
Rules
  ↓ baseline
Rules + Sliding Window
  ↓
TCN
  ↓
ST-GCN / PoseC3D comparison
```

只有离线指标和闭环指标都变好才替换 Baseline。

---

## Stage 9 — Edge / Real Drone Migration

```text
training runtime
      ↓
ONNX
      ↓
TensorRT
      ↓
Jetson
      ↓
PX4 real flight controller
      ↓
real quadrotor
```

此时视觉协议、Safety FSM、Control Gateway 应尽量保持不变。

---

# 22. SITL / HITL / 真机的定位

## SITL — 主开发环境

PX4 飞控软件运行在开发机中，Gazebo 提供传感器和物理世界。

优点：

- 成熟；
- 快；
- 易自动化；
- 易日志；
- 易故障注入；
- 适合作为日常 CI / 回归环境。

**项目绝大多数研发都应在 SITL 完成。**

## HITL — 后期可选

PX4 官方当前文档明确提示 HITL 属于社区维护，当前版本可能并不始终工作；当前 HITL 文档主要围绕 Gazebo Classic / jMAVSim。

参考：

https://docs.px4.io/main/en/simulation/hitl

所以 HITL 不作为主线阻塞项。

## Real Flight — 最终 Gate

当：

```text
SITL regression PASS
Safety regression PASS
real camera hybrid PASS
```

才逐步开放真机：

```text
HOVER
LEFT/RIGHT
ASCEND/DESCEND
```

再逐步增加复杂动作。

---

# 23. 真机迁移时什么不应该变

从：

```text
PX4 SITL ↔ Gazebo
```

换成：

```text
PX4 real firmware ↔ real quadrotor
```

理想情况下以下模块不变：

```text
Camera abstraction
Pose
Tracking
Operator Lock
Normalize
Gesture
Unknown Reject
Safety FSM
IntentMessage
Control Gateway high-level API
Logging schema
Evaluation metrics
```

真正需要替换 / 调整：

```text
transport
PX4 target configuration
camera driver
coordinate calibration
real-world safety limits
vehicle parameters
network / serial links
```

这就是为什么一开始必须保持模块边界。

---

# 24. 真正适合做成比赛创新点的部分

基础飞控和 Gazebo 不是创新点，它们是基础设施。

我们的自主价值集中在：

## 24.1 Operator Ownership Protocol

解决：

> 多人环境中“谁有权控制无人机”。

包括：

- acquisition；
- ownership lock；
- bystander rejection；
- operator lost；
- reacquisition；
- ownership release。

---

## 24.2 Open-set Gesture Rejection

不仅分类：

```text
LEFT / RIGHT / UP ...
```

还必须学习：

```text
UNKNOWN
```

核心指标：False Acceptance Rate。

---

## 24.3 Temporal Intent Recognition

从：

```text
单帧 Pose → 指令
```

升级到：

```text
人体连续行为
+ 时间稳定性
+ 当前系统状态
+ operator identity
       ↓
Control Intent
```

---

## 24.4 Safety-aware Human-Drone Interaction

```text
Gesture
+
confidence
+
stable time
+
operator ownership
+
vision quality
+
drone state
       ↓
Intent / Reject
```

系统的“拒绝”能力和“识别”能力同等重要。

---

## 24.5 可重复的 Hybrid Simulation Benchmark

如果能够形成标准化场景：

```text
真实摄像头测试
+ Gazebo actor regression
+ PX4 flight response
+ fault injection
```

并输出统一指标，这本身就是非常有说服力的工程成果。

---

# 25. 当前第一阶段任务优先级

| 优先级 | 工作 | 目标 |
|---|---|---|
| P0 | Ubuntu 22.04.5 + ROS 2 Humble | 已冻结正式环境 |
| P0 | PX4 SITL + Gazebo X500 | 数字四轴 |
| P0 | QGroundControl | 飞行监控与安全控制 |
| P0 | ROS 2 ↔ PX4 | 通信链 |
| P0 | Offboard baseline | 起飞/悬停/移动/降落 |
| P0 | 时间同步 + logging | 后续所有指标基础 |
| P1 | USB Camera 接入 | 真实视觉入口 |
| P1 | MediaPipe / Pose baseline | 快速关键点 |
| P1 | Gesture rules | 可解释 baseline |
| P1 | Safety FSM | 视觉与飞控隔离 |
| P1 | Hybrid closed loop | 真人手势控制 Gazebo 四轴 |
| P2 | Tracking / Operator Lock | 多人安全 |
| P2 | Unknown Reject | 开放集安全 |
| P2 | Actor regression | 自动化测试 |
| P2 | RTMPose / TCN | 算法升级 |
| P3 | ST-GCN / PoseC3D | 高级对比实验 |
| P3 | TensorRT / Jetson | 边缘部署 |
| P3 | PX4 真机 | 最终迁移 |

---

# 26. 第一阶段正式验收场景

第一次真正有意义的全系统验收应该是：

```text
Gazebo X500 正常悬停
        ↓
真实 Camera 看到 A / B 两个人
        ↓
A 通过激活动作成为 operator
        ↓
B 做 RIGHT
        ↓
系统拒绝
        ↓
A 做 RIGHT
        ↓
动作稳定确认
        ↓
MOVE_RIGHT
        ↓
PX4 Offboard setpoint
        ↓
Gazebo X500 右移
        ↓
A 消失
        ↓
视觉 timeout / operator lost
        ↓
HOVER / safety path
```

若这一条完整通过：

> 项目主架构成立，之后主要工作变成“逐模块提高性能”，而不是继续修改系统骨架。

---

# 27. 推荐资料索引

## PX4 / Flight / Simulation

- PX4 ROS 2 User Guide  
  https://docs.px4.io/main/en/ros2/user_guide

- PX4 ROS 2 Offboard Control  
  https://docs.px4.io/main/en/ros2/offboard_control

- PX4 Gazebo Simulation  
  https://docs.px4.io/main/en/sim_gazebo_gz/

- PX4 Gazebo Vehicles  
  https://docs.px4.io/main/en/sim_gazebo_gz/vehicles

- PX4 HITL  
  https://docs.px4.io/main/en/simulation/hitl

- Gazebo ROS 2 Integration  
  https://gazebosim.org/docs/harmonic/ros2_overview/

- Gazebo Actors  
  https://gazebosim.org/docs/garden/actors/

## System Reference

- AerialRobotics-IITK / gesture-controlled-drone  
  https://github.com/AerialRobotics-IITK/gesture-controlled-drone

## Gesture / Tello

- mageSoft / tello-gesture-controlled  
  https://github.com/mageSoft/tello-gesture-controlled

- jinMori127 / Hand-Drone-Control  
  https://github.com/jinMori127/Hand-Drone-Control

- DJI historical Tello-Python  
  https://github.com/dji-sdk/Tello-Python

- DJITelloPy  
  https://github.com/damiafuentes/djitellopy

## Pose / Tracking

- MediaPipe Pose Landmarker  
  https://developers.google.com/edge/mediapipe/solutions/vision/pose_landmarker/python

- Ultralytics Tracking  
  https://docs.ultralytics.com/modes/track/

- MMPose / RTMPose  
  https://github.com/open-mmlab/mmpose

## Action Recognition

- MMAction2  
  https://github.com/open-mmlab/mmaction2

---

# 28. 最终路线一句话版本

> **先用 PX4 SITL + Gazebo Harmonic + ROS 2 建立一个真实飞控逻辑和四轴物理模型完整存在的数字无人机；再把真实摄像头、Pose、Tracking、Operator Lock、Gesture、Unknown Reject 和 Safety FSM 接入，通过统一 Intent / Control Gateway 控制 PX4 Offboard；所有基础控制和动力学交给成熟平台，团队把创新集中在安全、鲁棒、可解释的人机视觉交互；最后将同一套视觉与协议迁移到 Jetson + PX4 真机。**

---

# 29. 对原 V1 路线的保留与调整

## 保留

V1 中以下原则继续作为核心：

- 问题拆分优先于模型选择；
- 模块通过稳定接口连接；
- Pose / Tracker / Gesture / Drone 可替换；
- Operator Lock；
- UNKNOWN / INVALID；
- Temporal Stabilization；
- Safety FSM；
- 研究环境与 Runtime 分离；
- ONNX / TensorRT 最终部署。

## 调整

V1 中原本靠后的：

```text
Tello / 仿真闭环
PX4 / ROS2
```

现在前移为：

```text
Stage 0 = PX4 + Gazebo + ROS2 工程底座
```

Tello 从“第一阶段飞行设备”调整为：

```text
Optional Physical Demo Adapter
```

整个路线从：

> “先做视觉，最后接飞行器”

升级为：

> **“先拥有一个正式数字无人机，再让真实视觉逐层进入完整闭环。”**
